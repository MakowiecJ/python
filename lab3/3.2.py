L = [3, 5, 4] ; L = L.sort()
# Metoda L.sort() zwraca None, a więc przypisujemy None do zmiennej L

# x, y = 1, 2, 3
# Za dużo liczb w sekwencji, mamy dwie zmienne i 3 wartości

# X = 1, 2, 3 ; X[1] = 4
# Wartości krotek nie można zmieniać, należałoby użyć listy

# X = [1, 2, 3] ; X[3] = 4
# Lista X ma indeksy od 0 do 2 włącznie, nie posiada natomiast indeksu 3

# X = "abc" ; X.append("d")
# Napisów nie można modyfikować (w miejscu), nie ma metody append()

# L = list(map(pow, range(8)))
# Funkcja pow potrzebuje dwóch argumentów